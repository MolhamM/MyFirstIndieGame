using System.Collections;
using System.Collections.Generic;
using UnityEngine;

abstract class Book : MonoBehaviour
{
    
    [SerializeField]
    int pages;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
